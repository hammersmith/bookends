<%= form_for media_item, remote: true do |f| %>
  <%= f.number_field :quantity, id: '', min: 0 %>
  <%= f.submit 'Update' %>
  <%= link_to 'Delete', media_item_path(media_item), method: :delete %>
<% end %>