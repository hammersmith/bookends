<%= form_for work, html: { class: 'js-update-item-form' }, remote: true do |f| %>
  <%= f.submit 'Update' %>
  <%= link_to 'Remove', work_path(work), remote: true, method: :delete, data: { confirm: 'Are you sure?' }, class: 'js-remove-item' %>
<% end %>
